const cadena= require("./data").default;
function compara(cadena)
{
    var resultado="la cadena \""+ cadena+"\""; 
    if (cadena.length===0)
        {
        resultado="es invalida";
        }
        if else ((cadena===cadena.toUpperCase))
        {
            resultado="verdadero";
        }
        else
        {
            resultado="falso";

        }
    
}
        